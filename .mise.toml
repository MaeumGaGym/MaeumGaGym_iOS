[settings]
# configure `mise install` to always keep the downloaded archive
always_keep_download = false
always_keep_install = false
verbose = false

[plugins]

[tools]
tuist = "3.23.1" # https://github.com/tuist/tuist/releases